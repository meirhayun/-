import { initNav, initHeaderScroll, highlightActiveLink } from '../components/nav';
import { initReveal } from '../components/reveal';

initNav();
initHeaderScroll();
highlightActiveLink();
initReveal('.cert-card');
